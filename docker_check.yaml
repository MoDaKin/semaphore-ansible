---
- name: Uninstall Package
  hosts: all
  gather_facts: no
  ignore_unreachable: true
  any_errors_fatal: false
  tasks:
    - name: Remove "scopio" package
    ansible.builtin.apt:
      name: scopio
      state: absent
